export default function num(a) {
  // TODO custom domain name;
  if (typeof +a === 'number') {
    console.log(a);
    console.log('da a');
    const num1 = +a;
    } else {
    num1 = +0;
    return num1;
  }
}
